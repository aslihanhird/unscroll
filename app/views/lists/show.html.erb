<div class="container">
  <div class="mt-2 mb-5">
    <h1 class="text-end list"><%= @list.name %> list</h1>
  </div>
  <ul>
    <% @list.all_posts.each do |post| %>
    <div class="d-flex justify-content-between">
      <div class="d-flex align-item-left">
        <p class="me-3"><%= post.insta_profile_id %></p>
        <p><%= post.insta_profile_id %></p>
      </div>
      <div><a href="#" class="btn btn-dark"><i class="fa-solid fa-plus me-1"></i>Save</a></div>
    </div>
      <li class="single-post">
        <img src="<%= post.media_url %>" alt="A post" class="mb-2">
        <%= post.caption %>
      </li>
    <% end %>
  </ul>
</div>
